name: Check Docker Image

on:
  push:
    branches: [ main ]

jobs:
  check-docker-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18
      - name: Notify slack
        env:
          SLACK_BOT_TOKEN: a422442db86654dee262b466ced47d77
        uses: pullreminders/slack-action@master
        with:
          args: '{\"channel\":\"C058ZPTE80Y\",\"text\":\"Hello world\"}'
